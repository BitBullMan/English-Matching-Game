# 依赖
node_modules
.pnp
.pnp.js

# 构建产物
dist
dist-ssr
*.local
.vite

# 环境变量（含密钥）
.env
.env.local
.env.*.local

# 编辑器/系统
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Capacitor 原生工程（按团队习惯：若要协作开发可去掉这两行）
# ios/
# android/

# 预生成 mp3 — 商业化部署需要随代码一起 commit，让 CDN 自动分发
# public/audio  ← 注释掉，让 audio 文件进 git
